#if($hasErrors)'***Telerik OpenAccess - Reverse Engineering errors***#end
#foreach($error in $errors)"OpenAccess error: $error "#end
#foreach($assembly in $assemblies)'$assembly#end
Imports System.Collections.Generic
#foreach( $import in $imports )${import}#end
#if(${hasPackage})Namespace ${classPackage}#end
' Generated by Telerik OpenAccess
<Telerik.OpenAccess.Persistent#if($multipleField)(Identity:=GetType(#end#if($multipleField && $hasPackage)$classPackage.#end#if($multipleField)$className.ID))#end#if($singleField)(IdentityField:="$(pkField)")#end> _
Partial Public Class $className

#foreach( $variableDeclaration in $allVariableDeclaration )$variableDeclaration#end
    
#if ($multipleField)$objectIdentity#end
End Class
#if(${hasPackage})End Namespace#end